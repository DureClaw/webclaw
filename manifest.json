{
  "manifest_version": 3,
  "name": "webclaw — DureClaw browser node",
  "version": "0.1.0",
  "description": "Join a DureClaw fleet from your browser. The master brings the brain; this node brings browser hands (fetch/DOM) — CORS-free, always-on.",
  "permissions": ["offscreen", "storage"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js" },
  "action": { "default_popup": "popup.html", "default_title": "webclaw" }
}
